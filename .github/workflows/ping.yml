name: ping
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://jahongir-monitor-bot.onrender.com/health && echo ok && curl -s https://jahongir-monitor-bot.onrender.com/ | head -c 100
